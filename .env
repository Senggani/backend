# Server Configuration
PORT=3300
NODE_ENV=development
TZ=Asia/Jakarta

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/pm-module_db

# JWT Configuration
JWT_SECRET=my_secret_key
JWT_EXPIRES_IN=24h # JWT expires in 24 hours
JWT_COOKIE_EXPIRES_IN=24 # Cookie expires in 24 hours

# Security Configuration
RATE_LIMIT_WINDOW=1 # 1 minute window for rate limiting
RATE_LIMIT_MAX=1000 # 5 attempts per window

